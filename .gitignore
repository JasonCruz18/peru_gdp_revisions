# =====================================================================
# .gitignore
# =====================================================================
# This file defines which files and directories should be ignored by Git.
# The following folders are automatically generated or contain data
# that should NOT be committed to the repository (e.g., large files,
# confidential data, or intermediate outputs).
# =====================================================================


# ---------------------------------------------------------------------
# Generated datasets and working files
# ---------------------------------------------------------------------
/gdp_revisions_datasets/data/
/gdp_revisions_datasets/new_wr/
/gdp_revisions_datasets/old_wr/
/gdp_revisions_datasets/pdf/
/gdp_revisions_datasets/new_weekly_reports/
/gdp_revisions_datasets/old_weekly_reports/
/gdp_revisions_datasets/alert_track/


# ---------------------------------------------------------------------
# Notes:
# - All files and subdirectories within these folders are ignored.
# - The folder paths remain untracked, but if needed, empty placeholder
#   files (like `.gitkeep`) can be added to keep folder structure visible.
# ---------------------------------------------------------------------
